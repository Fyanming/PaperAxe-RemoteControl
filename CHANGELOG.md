# 更新日志

## [2.1.0] - 2026-08-07

### 新增

- 主页面新增“设置”按钮与外观设置面板，支持默认、静态、动态三种背景模式
- 静态背景支持内置配色与本地图片，动态背景支持极光渐变、流光背景、本地视频循环播放
- 背景设置自动保存到配置文件，下次启动自动恢复
- 视频背景播放失败或文件缺失时自动回退到极光渐变
- 新增软件图标（16/24/32/48/64/128/256 多尺寸），已接入程序、托盘和安装包

### 性能优化

- 远控端鼠标输入刷新从 UI 线程 DispatcherTimer 移到独立后台线程，界面繁忙时输入不再延迟
- AES-GCM 读写加密实例改为复用，减少每条消息的加解密开销
- 远控画面渲染调度优先级从 Background 提升到 Normal，避免画面更新被饿死

### Bug 修复

- 修复无边框窗口底部圆角被“连接日志”等不透明面板覆盖的问题，主框架统一使用圆角 RectangleGeometry 裁剪并随窗口尺寸更新
- 修复整窗亚克力模糊导致“直角包圆角”矩形背景的问题，主窗口改为透明外圈加圆角面板

## [2.0.0] - 2026-08-01

### 重大更新

- 接通被控端画面与音频流：此前捕获服务已存在但从未发送，远控窗口一直停留在“等待画面”，现已完整打通屏幕与声音回传链路
- 全链路加密：TCP 连接使用 ECDH 密钥协商 + AES-GCM 加密，未加密客户端无法建立会话
- 远控设置面板：新增设置按钮，支持隐私保护（被控端熄屏、虚拟显示器检测）与声音传输开关
- 实时延迟显示：远控窗口每秒通过 Ping/Pong 刷新毫秒延迟
- 流畅度优化：帧率上限提升到 60 FPS，鼠标移动按 60Hz 合并发送，画面后台解码且只渲染最新帧
- 网络优化：TCP 启用 NoDelay，收发缓冲区提高到 512KB
- 音频降级：未安装虚拟音频设备时直接回传系统默认输出声音，不再静音无声
- 安装程序：提供自包含 .NET 10 单文件安装包；完整版安装包安装前检查 .NET 10 Desktop Runtime 并提供下载链接

### Bug 修复

- 修复被控端画面与音频从未发送给远控端的问题
- 修复屏幕缩放模式会裁剪画面而不是等比缩放的问题
- 修复被控端停止监听后无法再次启动的问题
- 修复远控操作“隔一会才有反应”的问题：帧积压导致 UI 线程阻塞、输入发送被画面解码拖慢
- 修复没有虚拟声卡时被控端静音且控制端听不到声音的问题
- 修复音频初始化卡住会话建立的问题，COM/RCW 异常不再阻塞画面与输入
- 修复点击、滚轮、按键前鼠标位置可能未刷新，导致操作落在旧坐标的问题
- 修复安装程序静默安装时 `/DIR` 参数被覆盖、桌面快捷方式缺失的问题
